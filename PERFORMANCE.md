# 性能验证

任何性能优化必须遵循以下顺序：

1. 明确待优化指标和正确性约束。
2. 增加可复现的基线测试，保留旧路径作为测试基线。
3. 在同一数据库镜像、硬件、数据集和事务边界下重复测量。
4. 一次只修改一个变量。
5. 运行正确性测试、竞态测试和真实 Kingbase 集成测试。
6. 重复性能测试，用 `benchstat` 或中位数报告结果；没有测量结果不得宣称性能提升。

## 批量 upsert

测试入口：

```bash
make benchmark-unit
make benchmark-integration
# 或依次运行全部基准
make benchmark
```

测试环境：

- CPU：Intel Core Ultra 5 125H
- Kingbase 镜像：`lihongjie0209/kingbase:latest`
- 数据：1000 行，4 列，单列主键
- 基线：事务内预编译 SQL 后逐行执行 upsert
- 候选：单事务内按 50、100、250、500、1000 行执行多行 upsert
- 容器启动、建表和每轮 TRUNCATE 不计时
- 每轮结束校验目标表行数

2026-09-14 复测结果（每个场景 10 次，每次执行 3 轮，表中为中位数）：

| 场景 | 1000 行耗时 | 吞吐 | 分配内存 | 分配次数 |
| --- | ---: | ---: | ---: | ---: |
| 逐行基线 | 351.5 ms | 约 2,846 rows/s | 33.14 MB | 491,053 |
| 批量 50 | 43.8 ms | 约 22,981 rows/s | 22.41 MB | 67,626 |
| 批量 100 | 47.1 ms | 约 21,269 rows/s | 40.69 MB | 65,860 |
| 批量 250 | 70.1 ms | 约 14,295 rows/s | 94.37 MB | 64,772 |
| 批量 500 | 121.7 ms | 约 8,215 rows/s | 211.68 MB | 68,407 |
| 批量 1000 | 199.2 ms | 约 5,020 rows/s | 447.30 MB | 70,231 |

批量 50 相对逐行基线：

- 中位耗时降低约 87.5%
- 吞吐约提升 8.0 倍
- 分配内存降低约 32.4%
- 分配次数降低约 86.2%

Kingbase 驱动处理超长参数语句时内存增长明显，因此多行参数 SQL 的推荐批次是 50。随后进一步测试了 COPY 暂存表方案。

### COPY 暂存表合并

测试仍为 1000 行、10 次、每次 3 轮：

| 场景 | 中位耗时 | 分配内存 | 分配次数 |
| --- | ---: | ---: | ---: |
| 多行 upsert，批量 50 | 48.7 ms | 22.41 MB | 67,628 |
| COPY 暂存表后合并 | 16.4 ms | 0.389 MB | 6,077 |

COPY 暂存表相对批量 50：

- 中位耗时降低约 66.3%
- 吞吐约提升 2.97 倍
- 分配内存降低约 98.3%
- 分配次数降低约 91.0%

该候选通过后，生产增量路径才修改为“事务内临时暂存表 → COPY → `INSERT SELECT ON CONFLICT`”。由于 COPY 不再构造海量参数，默认 `batch_size` 设置为 1000。

### 跳过无变化 UPDATE

相同数据重复 upsert 的测试中，增加 `IS DISTINCT FROM` 条件的中位耗时与无条件版本基本持平（约 56.5 ms 对 57.1 ms），内存由约 22.4 MB 增至 27.9 MB，分配次数增加约 10%。当前证据没有证明它是性能优化，因此没有合入生产路径。若未来能采集 Kingbase WAL 字节数，可重新评估其写放大收益。

## 多表并发

测试包含 4 张相互独立的表，每张 5000 行，测量完整 `Full()` 路径（元数据检查、统一清理和 COPY），每个场景 5 次、每次 3 轮：

| 并发度 | 中位耗时 | 相对并发 1 |
| ---: | ---: | ---: |
| 1 | 372.9 ms | 基线 |
| 2 | 267.3 ms | 耗时降低 28.3%，吞吐约 1.40 倍 |
| 4 | 284.3 ms | 耗时降低 23.7%，但比并发 2 慢 6.4% |

因此将默认 `table_parallelism` 设置为 2。并发 4 已出现数据库/连接竞争的边际退化，不采用更激进默认值。不同磁盘与远程网络环境仍应重新测试 1、2、4、8。

在同一基准中加入“全量任务内复用预检元数据”后再次以 5 次、每次 3 轮复测：并发 1/2/4 的中位耗时分别为 230.9/192.4/188.6 ms，较优化前分别降低约 38.1%/28.0%/33.7%。并发 4 只比并发 2 快约 2.0%，但分配更多且更易争抢连接，因此默认值仍保持 2。

## 无主键游标组合批次

测试 200 行、每行具有不同游标值，比较每游标一个事务和多个游标共用事务，每个场景 5 次、每次 3 轮：

| 每事务游标数 | 中位耗时 | 吞吐 | 分配内存 |
| ---: | ---: | ---: | ---: |
| 1 | 1.021 s | 约 196 rows/s | 14.06 MB |
| 10 | 110.4 ms | 约 1,812 rows/s | 1.55 MB |
| 50 | 20.3 ms | 约 9,841 rows/s | 0.557 MB |

游标组 50 相对基线约快 50 倍，内存减少约 96%，分配次数减少约 87%，因此新增 `no_key_cursor_batch` 并默认设置为 50。

## 全量索引创建时机

5000 行、一个复合二级索引的完整建表/COPY/建索引测试：索引先建中位约 48.9 ms，COPY 后建约 44.6 ms，耗时降低约 8.7%，内存基本不变。因此全量同步中缺失的普通/唯一索引改为 COPY 完成后创建；已有目标索引不会删除。

## 全量扫描缓冲区复用

纯 Go 热路径基准使用 24 列、1000 行，5 轮、每轮 2 秒。逐行创建扫描切片的中位结果约为 1.13 ms、384 KB、1000 次分配；每个 COPY 流复用一组扫描切片约为 0.155 ms、384 B、1 次分配。CPU 时间约降低 86%，缓冲区分配约降低 99.9%，因此全量 COPY 循环改为复用扫描缓冲区。数据库驱动的端到端结果仍以集成基准为准。

## 断点持久化频率

每轮模拟 100 个已提交批次并写入同一状态文件，3 次、每次 3 轮：每批 `fsync` 的中位耗时约 556 ms；每 10 批约 57.0 ms，降低约 89.7%；每 50 批约 11.0 ms，降低约 98.0%。默认采用较保守的 `checkpoint_every_batches: 10`，并在每张表退出（包括错误返回）前强制持久化。进程被强制终止时，最近不足 10 个、已经提交的批次可能被安全重放；主键 upsert 和无主键游标组替换均为幂等操作。

## 增量断点提取

1000 行、2 个排序字段的纯 Go 基准中，逐行创建断点切片的中位耗时约 230 µs、66.1 KB、3000 次分配；批次内复用切片约 157 µs、34.1 KB、2001 次分配。耗时降低约 31.7%，内存降低约 48.4%，分配次数降低约 33.3%，因此 `readBatch` 改为复用最终断点切片并预计算排序列下标。

## 增量元数据快照复用

真实 Kingbase 系统目录基准使用一张复合主键及二级索引表，10 次、每次 3 轮。当前重复预检的中位耗时约 36.4 ms、1.71 MB、12,339 次分配；单次任务内复用预检快照约 23.5 ms、1.14 MB、8,234 次分配。耗时降低约 35.3%，内存降低约 33.3%，分配次数降低约 33.3%。因此增量任务的全表预检结果会复用于随后的数据阶段；快照不会跨命令调用保存，下次运行仍会重新检测结构变化。

## 全量外键创建时机

真实 Kingbase 基准使用 10,000 行父表和 10,000 行子表，5 次、每次 3 轮。外键先创建再 COPY 的中位耗时约 114.1 ms，COPY 完成后创建约 67.3 ms，降低约 41.0%，内存和分配次数基本不变。因此全量初次同步的缺失外键改为数据装载后创建，由 Kingbase 在建约束时一次性验证已有数据；已有目标外键不会删除，增量模式仍在写入前完成外键对比。
